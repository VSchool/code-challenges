function sortedOfAge(arr) {
    // Return a list of everyone older than 18.
    // Have the new list be sorted by last name.
    // Return each name and age with <li>'s.
}

module.exports = {
    sortedOfAge
}
