//your objects(s) here

// Create and declare 10 objects from things you see. Name each one with const, let, or var.
// Each object should have at least 3 keys (properties).
// Have at least one boolean, string, number, array, nested object, etc.
// Each object should have at least one method.

// list obejects in this array and run tests to test

const objectArray = ["PLACE OBJECTS HERE. Separate objects with commas"]

module.exports = objectArray
