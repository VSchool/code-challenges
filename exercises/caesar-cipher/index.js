// Install and require readline-sync
    // Request input and shift amount from user using readline-sync
    // Give the input and shift amount to the cipher function and return the shifted input


function caeserCipher(input, shift){
    // Your code here
}

module.exports = { caeserCipher }
