// Make a function that takes a string and returns that string reversed.
const reverseStr = function(str) {

}


// Make a function that takes a string and returns true if the string 
// could be a number else return false.
const isNum = function(str) {

}


// Make a function that takes a number and returns true if the number 
// is even else return false.
const isEven = function(num) {
  
}


// Make a function that takes an array and returns the average of the array.
const averageArray = function(arr) {
  
}


// Make a function that takes two arrays and returns a single array of the items 
// from the arrays added alternatingly.
const combineArrays = function(arr1, arr2) {

}


module.exports = {
    reversedStr,
    isNum,
    isEven,
    averageArray,
    combineArrays
}